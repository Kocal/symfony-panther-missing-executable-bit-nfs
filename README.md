# Symfony Panther: missing executable bit on binary in VM (NFS)

## Requirements

- Vagrant 2+
- VirtualBox 5.2+

## Installation

```bash
# Outside the VM
$ make setup
$ vagrant up && vagrant ssh

# In the VM
$ composer install
```
